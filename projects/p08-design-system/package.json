{
  "name": "p08-design-system",
  "private": true,
  "version": "1.0.0",
  "description": "A type-safe design-system core: class merging + variant API (CVA-style).",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:types": "vitest run --typecheck",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
